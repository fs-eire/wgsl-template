{
  "type": "generator",
  "description": "Test error handling for multiple #else blocks in the same #if chain",
  "entries": {
    "test.wgsl.template": {
      "targets": {
        "test": {
          "expectsError": "#else mismatch"
        }
      }
    }
  }
}
