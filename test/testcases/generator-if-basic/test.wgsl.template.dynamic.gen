emit("fn my_func(value: f32) {\n");
if (param["ENABLE_FACTOR"]) {
emit("    return value + 0.1;\n");
} else {
emit("    let factor = 2.5;\n");
emit("    return value * factor + 0.1;\n");
}
emit("}\n");
