emit("fn compute_kernel() {\n");
emit("    let global_idx = get_global_id(0u);\n");
emit("\n");
emit("    if (global_idx < ");
emit(param["BUFFER_SIZE"]);
emit(") {\n");
emit("\n");
emit("        ");
emit(variable["b"].SetByIndices(`${variable["a"].OffsetToIndices("global_idx")}`, "input_data[global_idx] * 2.0"));
emit(";\n");
emit("\n");
emit("        ");
emit(variable["output_buffer"].SetByIndices(`${variable["tensor_a"].OffsetToIndices("global_idx")} + 1`, `f(${variable["input_buffer"].GetByIndices(`${variable["tensor_b"].OffsetToIndices("global_idx + 1u")}`)})`));
emit(";\n");
emit("\n");
emit("        let indices = ");
emit(variable["a"].OffsetToIndices("global_idx"));
emit(";\n");
emit("        ");
emit(variable["b"].SetByIndices("indices", "42.0"));
emit(";\n");
emit("    }\n");
emit("}\n");